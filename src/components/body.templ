package components

templ Body() {
	@Layout() {
		<div class="flex flex-col h-full w-full bg-red-50">
			<div class="mx-auto my-auto">
				<span>Body Here</span>
                <button onclick="alert(window.stake.sum(1,2));">Click me</button>
			</div>
		</div>
	}
}
